Currently configured to only work on one domain. However, user can clone code and change url's to match desired suite.

Purpose: to quickly pull metrics per test case status. 

Future updates will include ability to identify existing test cases status' and pull metrics based on such.